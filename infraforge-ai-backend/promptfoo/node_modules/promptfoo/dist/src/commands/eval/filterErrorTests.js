"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.filterErrorTests = filterErrorTests;
const types_1 = require("../../types");
const util_1 = require("../../util");
async function filterErrorTests(testSuite, outputPath) {
    if (!testSuite.tests) {
        return [];
    }
    const { results } = await (0, util_1.readOutput)(outputPath);
    const errorResults = results.results.filter((result) => result.failureReason === types_1.ResultFailureReason.ERROR);
    if (errorResults.length === 0) {
        return [];
    }
    return [...testSuite.tests].filter((test) => {
        return errorResults.some((result) => (0, util_1.resultIsForTestCase)(result, test));
    });
}
//# sourceMappingURL=filterErrorTests.js.map