# Core backend — works on Python 3.12+ and 3.14
fastapi
uvicorn
python-multipart

# Database
motor
pymongo

# Config / env
python-dotenv
pydantic

# AI / LLM clients
groq
openai

# Embeddings / ML
sentence-transformers
numpy

# Fuzzy spelling tolerance
rapidfuzz

# Image preprocessing (OpenCV — used by image search pipeline)
opencv-python-headless
pillow

# PDF processing (RAG)
pypdf

# Optional extras (TensorFlow MobileNet fallback, YOLO) — see requirements-ml-extras.txt
# TensorFlow does NOT support Python 3.14; install extras only on Python 3.11–3.12.
